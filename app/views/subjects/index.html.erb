<h3 class="text-center mt-5">Lista de materias</h3>
<div class="row">
    <div class="col-md-10 mt-5 mx-auto">
        <div class="card shadow-lg">
            <div class="card-body">
                <div class="box">
                    <%= link_to "Generar materia", new_user_subject_path(@user, @subject), class: "mb-2 btn btn-info btn-sm" %>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <th>Codígo</th>
                                <th>Nombre</th>
                                <th>Editar</th>
                                <th>Eliminar</th>
                            </thead>
                            <tbody>
                                <%= render @subjects %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
